<?php
// Защита от прямого доступа к файлу
defined('ACCESS') or die('Access denied');

// Параметры подключения к базе данных MySQL
$mysqlhost = "localhost";       // Хост базы данных (локальный сервер по умолчанию)
$mysqluser = "root";            // Имя пользователя базы данных (для разработки)
$mysqlpass = "";                // Пароль пользователя (пустой — только для локальной разработки)
$mysqlbase = "games_bd";        // Имя базы данных (например, "aloneislands")

// Рекомендация: Используйте переменные окружения (.env) для продакшена
// Пример: $mysqlpass = getenv('DB_PASSWORD') ?: "";
?>