<?php
// Защита от прямого доступа к файлу
defined('ACCESS') or die('Access denied');

// Параметры подключения к базе данных MySQL
$mysqlhost = "db";       // Хост базы данных (локальный сервер по умолчанию)
$mysqluser = "game_user";            // Имя пользователя базы данных (для разработки)
$mysqlpass = "game_password";                // Пароль пользователя (пустой — только для локальной разработки)
$mysqlbase = "aloneislands";        // Имя базы данных (например, "aloneislands")

// Рекомендация: Используйте переменные окружения (.env) для продакшена
// Пример: $mysqlpass = getenv('DB_PASSWORD') ?: "";
?>